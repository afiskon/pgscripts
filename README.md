# pgscripts
A few PostgreSQL-related scripts.

Most interesting scripts are install.sh and install-single.sh.

Typical usage:

```
./quick-build.sh
# or ./full-build.sh
./install.sh
make installcheck
```
